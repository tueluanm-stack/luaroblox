-- =============================================================================
-- [ PHẦN CẤU HÌNH MENU - BẠN CHỈ CẦN CHỈNH SỬA TẠI ĐÂY ]
-- =============================================================================
local MenuTitle = "MENU PRO BY GEMINI" -- Tên hiển thị trên thanh Menu

local MenuConfig = {
    {
        Name = "Kích Hoạt Nhảy Cao",
        Script = function()
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 150
            print("Đã bật Nhảy Cao!")
        end
    },
    {
        Name = "Kích Hoạt Chạy Nhanh",
        Script = function()
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 100
            print("Đã bật Chạy Nhanh!")
        end
    },
    {
        Name = "Nhận Bản Đồ / Tool",
        Script = function()
            print("Đã chạy script bên thứ ba thành công!")
        end
    },
    {
        Name = "Nút Poine Băng",
        Script = function()
            local PATH_POSITIONS = {
                Vector3.new(-55.39, 202.12, -48.71),
                Vector3.new(86.31, 202.12, -35.57),
                Vector3.new(-35.48, 202.12, -10.70),
                Vector3.new(67.23, 202.18, -10.35),
                Vector3.new(18.54, 202.12, -50.85),
                Vector3.new(13.40, 202.12, -44.00),
                Vector3.new(17.40, 202.12, -44.00),
                Vector3.new(22.40, 202.12, -44.00),
                Vector3.new(17.40, 202.12, -40.00),
                Vector3.new(85.89, 202.12, -60.25),
                Vector3.new(65.89, 202.12, -60.25),
                Vector3.new(65.01, 202.12, -38.78),
            }
            local CAMERA_LOOK = Vector3.new(0.00, -0.98, -0.17)
            local Players = game:GetService("Players")
            local Workspace = game:GetService("Workspace")
            local LocalPlayer = Players.LocalPlayer
            local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")
            pcall(function()
                if PlayerGui:FindFirstChild("TeleportCycleGui") then
                    PlayerGui.TeleportCycleGui:Destroy()
                end
            end)
            local ScreenGui = Instance.new("ScreenGui")
            ScreenGui.Name = "TeleportCycleGui"
            ScreenGui.ResetOnSpawn = false
            ScreenGui.Parent = PlayerGui
            local TeleportButton = Instance.new("TextButton")
            TeleportButton.Size = UDim2.new(0, 105, 0, 40)
            TeleportButton.Position = UDim2.new(0.05, 0, 0.4, 0)
            TeleportButton.BackgroundColor3 = Color3.fromRGB(255, 85, 85)
            TeleportButton.BorderSizePixel = 0
            TeleportButton.TextColor3 = Color3.fromRGB(255,255,255)
            TeleportButton.Font = Enum.Font.SourceSansBold
            TeleportButton.TextSize = 16
            TeleportButton.Active = true
            TeleportButton.Draggable = true
            TeleportButton.AutoButtonColor = true
            TeleportButton.Parent = ScreenGui
            local UICorner = Instance.new("UICorner")
            UICorner.CornerRadius = UDim.new(0, 10)
            UICorner.Parent = TeleportButton
            local UIStroke = Instance.new("UIStroke")
            UIStroke.Thickness = 1.5
            UIStroke.Color = Color3.fromRGB(255,255,255)
            UIStroke.Parent = TeleportButton
            local currentPoint = 1
            local isTeleporting = false
            local function updateButtonText()
                TeleportButton.Text = "Point " .. tostring(currentPoint)
            end
            updateButtonText()
            local function teleportToNextPoint()
                if isTeleporting then return end
                isTeleporting = true
                local character = LocalPlayer.Character
                if not character then isTeleporting = false return end
                local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")
                local camera = Workspace.CurrentCamera
                if humanoidRootPart and camera then
                    local targetPosition = PATH_POSITIONS[currentPoint]
                    humanoidRootPart.CFrame = CFrame.new(targetPosition)
                    task.wait(0.05)
                    LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson
                    local camPos = camera.CFrame.Position
                    camera.CFrame = CFrame.lookAt(camPos, camPos + CAMERA_LOOK)
                    currentPoint += 1
                    if currentPoint > #PATH_POSITIONS then currentPoint = 1 end
                    updateButtonText()
                end
                isTeleporting = false
            end
            TeleportButton.MouseButton1Click:Connect(function()
                teleportToNextPoint()
            end)
            LocalPlayer.CharacterAdded:Connect(function()
                task.wait(1)
                LocalPlayer.CameraMode = Enum.CameraMode.Classic
            end)
        end
    },
    {
        Name = "Nút Update 12",
        Script = function()
            local Players = game:GetService("Players")
            local CoreGui = game:GetService("CoreGui")
            local UserInputService = game:GetService("UserInputService")
            local localPlayer = Players.LocalPlayer
            local playerGui = localPlayer:WaitForChild("PlayerGui")
            local isAutoUpgrading = false
            local screenGui = Instance.new("ScreenGui")
            screenGui.Name = "AbsoluteUpgradeGui12"
            pcall(function() screenGui.Parent = CoreGui end)
            if not screenGui.Parent then screenGui.Parent = playerGui end
            local mainButton = Instance.new("TextButton")
            mainButton.Size = UDim2.new(0, 140, 0, 45)
            mainButton.Position = UDim2.new(0.1, 0, 0.4, 0)
            mainButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
            mainButton.Text = "Auto 1-12: OFF"
            mainButton.TextColor3 = Color3.fromRGB(255,255,255)
            mainButton.Font = Enum.Font.GothamBold
            mainButton.TextSize = 13
            mainButton.Active = true
            mainButton.Parent = screenGui
            local dragging, dragInput, dragStart, startPos = false, nil, nil, nil
            local function updateDrag(input)
                local delta = input.Position - dragStart
                mainButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            end
            mainButton.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true; dragInput = input; dragStart = input.Position; startPos = mainButton.Position
                    input.Changed:Connect(function()
                        if input.UserInputState == Enum.UserInputState.End then dragging = false; dragInput = nil end
                    end)
                end
            end)
            mainButton.InputChanged:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
            end)
            UserInputService.InputChanged:Connect(function(input)
                if dragging and input == dragInput then updateDrag(input) end
            end)
            local function forceClick(button)
                if button then
                    pcall(function()
                        for _, c in pairs(getconnections(button.MouseButton1Click)) do c:Fire() end
                        for _, c in pairs(getconnections(button.MouseButton1Down)) do c:Fire() end
                        for _, c in pairs(getconnections(button.Activated)) do c:Fire() end
                    end)
                end
            end
            local function getUpgradeButton(towerIndex)
                local targetPath = {"MainHud","Menus","TeamManager","Menu","Towers","Listings",tostring(towerIndex),"UpgradeButtons","Upgrade"}
                local current = playerGui
                for _, name in ipairs(targetPath) do
                    if current then current = current:FindFirstChild(name) else return nil end
                end
                return current
            end
            task.spawn(function()
                while true do
                    if isAutoUpgrading then
                        for i = 1, 12 do
                            if isAutoUpgrading then
                                local btn = getUpgradeButton(i)
                                if btn then forceClick(btn) end
                                task.wait(0.3)
                            end
                        end
                    else
                        task.wait(0.5)
                    end
                end
            end)
            mainButton.MouseButton1Click:Connect(function()
                if dragging then return end
                isAutoUpgrading = not isAutoUpgrading
                if isAutoUpgrading then
                    mainButton.BackgroundColor3 = Color3.fromRGB(46,204,113)
                    mainButton.Text = "Auto 1-12: ON"
                else
                    mainButton.BackgroundColor3 = Color3.fromRGB(231,76,60)
                    mainButton.Text = "Auto 1-12: OFF"
                end
            end)
        end
    },
    {
        Name = "Nút Update 6",
        Script = function()
            local Players = game:GetService("Players")
            local CoreGui = game:GetService("CoreGui")
            local UserInputService = game:GetService("UserInputService")
            local localPlayer = Players.LocalPlayer
            local playerGui = localPlayer:WaitForChild("PlayerGui")
            local isAutoUpgrading = false
            local screenGui = Instance.new("ScreenGui")
            screenGui.Name = "AbsoluteUpgradeGui6"
            pcall(function() screenGui.Parent = CoreGui end)
            if not screenGui.Parent then screenGui.Parent = playerGui end
            local mainButton = Instance.new("TextButton")
            mainButton.Size = UDim2.new(0, 140, 0, 45)
            mainButton.Position = UDim2.new(0.1, 0, 0.5, 0)
            mainButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
            mainButton.Text = "Auto 1-6: OFF"
            mainButton.TextColor3 = Color3.fromRGB(255,255,255)
            mainButton.Font = Enum.Font.GothamBold
            mainButton.TextSize = 13
            mainButton.Active = true
            mainButton.Parent = screenGui
            local dragging, dragInput, dragStart, startPos = false, nil, nil, nil
            local function updateDrag(input)
                local delta = input.Position - dragStart
                mainButton.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
            end
            mainButton.InputBegan:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
                    dragging = true; dragInput = input; dragStart = input.Position; startPos = mainButton.Position
                    input.Changed:Connect(function()
                        if input.UserInputState == Enum.UserInputState.End then dragging = false; dragInput = nil end
                    end)
                end
            end)
            mainButton.InputChanged:Connect(function(input)
                if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
            end)
            UserInputService.InputChanged:Connect(function(input)
                if dragging and input == dragInput then updateDrag(input) end
            end)
            local function forceClick(button)
                if button then
                    pcall(function()
                        for _, c in pairs(getconnections(button.MouseButton1Click)) do c:Fire() end
                        for _, c in pairs(getconnections(button.MouseButton1Down)) do c:Fire() end
                        for _, c in pairs(getconnections(button.Activated)) do c:Fire() end
                    end)
                end
            end
            local function getUpgradeButton(towerIndex)
                local targetPath = {"MainHud","Menus","TeamManager","Menu","Towers","Listings",tostring(towerIndex),"UpgradeButtons","Upgrade"}
                local current = playerGui
                for _, name in ipairs(targetPath) do
                    if current then current = current:FindFirstChild(name) else return nil end
                end
                return current
            end
            task.spawn(function()
                while true do
                    if isAutoUpgrading then
                        for i = 1, 6 do
                            if isAutoUpgrading then
                                local btn = getUpgradeButton(i)
                                if btn then forceClick(btn) end
                                task.wait(0.3)
                            end
                        end
                    else
                        task.wait(0.5)
                    end
                end
            end)
            mainButton.MouseButton1Click:Connect(function()
                if dragging then return end
                isAutoUpgrading = not isAutoUpgrading
                if isAutoUpgrading then
                    mainButton.BackgroundColor3 = Color3.fromRGB(46,204,113)
                    mainButton.Text = "Auto 1-6: ON"
                else
                    mainButton.BackgroundColor3 = Color3.fromRGB(231,76,60)
                    mainButton.Text = "Auto 1-6: OFF"
                end
            end)
        end
    },
    {
        Name = "Nút 4 Tổ Hợp",
        Script = function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/tueluanm-stack/luaroblox/refs/heads/main/thehouse4"))()
        end
    },
    {
        Name = "Nút AFK",
        Script = function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/tueluanm-stack/luaroblox/refs/heads/main/Players.LocalPlayer.AntiAfk"))()
        end
    },
    {
        Name = "Nút Thử Nghiệm 9",
        Script = function()
            print("Script 9 hoạt động!")
        end
    },
    -- ✅ Thêm nút mới: chỉ cần copy khối {} này xuống dưới, đổi Name và Script
}

-- =============================================================================
-- [ GUI ENGINE - CÁC SỬA QUAN TRỌNG ]
-- FIX 1: Drag chỉ gắn trên TitleBar thay vì toàn MainFrame
-- FIX 2: Thêm UIPadding cho ScrollFrame để nút không bị cắt
-- FIX 3: Chiều cao MainFrame tự động theo số nút (có giới hạn tối đa)
-- =============================================================================

local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")
local CoreGui = game:GetService("CoreGui")
local LocalPlayer = Players.LocalPlayer

local TargetParent = CoreGui or LocalPlayer:WaitForChild("PlayerGui")

if TargetParent:FindFirstChild("Gemini_CustomMenu") then
    TargetParent["Gemini_CustomMenu"]:Destroy()
end

-- Tính chiều cao hợp lý: mỗi nút 42px + 8px padding, tối đa 400px
local BUTTON_H = 42
local BUTTON_GAP = 8
local PADDING_V = 10 -- padding trên/dưới trong scroll
local TITLE_H = 50
local MAX_FRAME_H = 420
local MIN_FRAME_H = 160
local totalContentH = (#MenuConfig * (BUTTON_H + BUTTON_GAP)) - BUTTON_GAP + PADDING_V * 2
local frameH = math.clamp(totalContentH + TITLE_H, MIN_FRAME_H, MAX_FRAME_H)

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Gemini_CustomMenu"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = TargetParent

-- Khung chính
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 300, 0, frameH)
MainFrame.Position = UDim2.new(0.5, -150, 0.5, -frameH / 2)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true -- ✅ Tránh nút bị tràn ra ngoài frame
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

-- Thanh tiêu đề (chỉ drag ở đây)
local TitleBar = Instance.new("Frame")
TitleBar.Name = "TitleBar"
TitleBar.Size = UDim2.new(1, 0, 0, TITLE_H)
TitleBar.Position = UDim2.new(0, 0, 0, 0)
TitleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleBar.BorderSizePixel = 0
TitleBar.Parent = MainFrame

local TitleCorner = Instance.new("UICorner")
TitleCorner.CornerRadius = UDim.new(0, 10)
TitleCorner.Parent = TitleBar

-- Che góc dưới của TitleBar để không lộ góc bo tròn
local TitleFix = Instance.new("Frame")
TitleFix.Size = UDim2.new(1, 0, 0, 10)
TitleFix.Position = UDim2.new(0, 0, 1, -10)
TitleFix.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleFix.BorderSizePixel = 0
TitleFix.Parent = TitleBar

local TitleLabel = Instance.new("TextLabel")
TitleLabel.Size = UDim2.new(1, -50, 1, 0)
TitleLabel.Position = UDim2.new(0, 12, 0, 0)
TitleLabel.BackgroundTransparency = 1
TitleLabel.Text = MenuTitle
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 15
TitleLabel.Font = Enum.Font.SourceSansBold
TitleLabel.TextXAlignment = Enum.TextXAlignment.Left
TitleLabel.Parent = TitleBar

-- Nút đóng (X)
local CloseButton = Instance.new("TextButton")
CloseButton.Size = UDim2.new(0, 28, 0, 28)
CloseButton.Position = UDim2.new(1, -36, 0.5, -14)
CloseButton.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
CloseButton.Text = "×"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 20
CloseButton.Font = Enum.Font.SourceSansBold
CloseButton.Parent = TitleBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

-- ✅ FIX: ScrollingFrame chiếm phần còn lại bên dưới TitleBar
local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Name = "ScrollFrame"
ScrollFrame.Size = UDim2.new(1, 0, 1, -TITLE_H)
ScrollFrame.Position = UDim2.new(0, 0, 0, TITLE_H)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 4
ScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(120, 120, 120)
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.AutomaticCanvasSize = Enum.AutomaticCanvasSize.Y
ScrollFrame.ScrollingEnabled = true
ScrollFrame.Parent = MainFrame

-- ✅ FIX: UIListLayout + UIPadding để nút không bị sát cạnh / bị cắt
local ListLayout = Instance.new("UIListLayout")
ListLayout.SortOrder = Enum.SortOrder.LayoutOrder
ListLayout.Padding = UDim.new(0, BUTTON_GAP)
ListLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center
ListLayout.Parent = ScrollFrame

local ListPadding = Instance.new("UIPadding")
ListPadding.PaddingTop = UDim.new(0, PADDING_V)
ListPadding.PaddingBottom = UDim.new(0, PADDING_V)
ListPadding.PaddingLeft = UDim.new(0, 8)
ListPadding.PaddingRight = UDim.new(0, 8)
ListPadding.Parent = ScrollFrame

-- Tạo nút tự động
for index, config in ipairs(MenuConfig) do
    local Button = Instance.new("TextButton")
    Button.Name = "ScriptButton_" .. index
    Button.Size = UDim2.new(1, 0, 0, BUTTON_H)
    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
    Button.Text = config.Name
    Button.TextColor3 = Color3.fromRGB(230, 230, 230)
    Button.TextSize = 14
    Button.Font = Enum.Font.SourceSansSemibold
    Button.AutoButtonColor = false
    Button.Parent = ScrollFrame

    local BtnCorner = Instance.new("UICorner")
    BtnCorner.CornerRadius = UDim.new(0, 6)
    BtnCorner.Parent = Button

    local isClicked = false

    Button.MouseButton1Click:Connect(function()
        if not isClicked then
            isClicked = true
            Button.BackgroundColor3 = Color3.fromRGB(20, 60, 35)
            Button.Text = "✓ " .. config.Name
            Button.TextColor3 = Color3.fromRGB(100, 220, 130)

            task.spawn(function()
                local success, err = pcall(config.Script)
                if not success then
                    warn("Lỗi nút [" .. config.Name .. "]: " .. tostring(err))
                    -- Hiển thị lỗi trên nút 2 giây rồi reset
                    Button.BackgroundColor3 = Color3.fromRGB(100, 20, 20)
                    Button.Text = "✗ Lỗi - " .. config.Name
                    Button.TextColor3 = Color3.fromRGB(255, 100, 100)
                    task.wait(2)
                    isClicked = false
                    Button.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
                    Button.Text = config.Name
                    Button.TextColor3 = Color3.fromRGB(230, 230, 230)
                end
            end)
        end
    end)
end

-- =============================================================================
-- [ DRAG SYSTEM - ✅ FIX: Chỉ kéo từ TitleBar ]
-- =============================================================================
local dragToggle = false
local dragStart = nil
local startPos = nil

TitleBar.InputBegan:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseButton1
        or input.UserInputType == Enum.UserInputType.Touch)
        and UIS:GetFocusedTextBox() == nil then
        dragToggle = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragToggle = false
            end
        end)
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragToggle and (input.UserInputType == Enum.UserInputType.MouseMovement
        or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        MainFrame.Position = UDim2.new(
            startPos.X.Scale,
            startPos.X.Offset + delta.X,
            startPos.Y.Scale,
            startPos.Y.Offset + delta.Y
        )
    end
end)

print("[Menu] Đã tải xong! Tổng " .. #MenuConfig .. " nút.")
