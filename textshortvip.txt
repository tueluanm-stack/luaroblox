-- =============================================================================
-- [ PHẦN CẤU HÌNH MENU - BẠN CHỈ CẦN CHỈNH SỬA TẠI ĐÂY ]
-- =============================================================================
local MenuTitle = "MENU PRO BY GLASS" -- Tên hiển thị trên thanh Menu

local MenuConfig = {
    {
        Name = "Kích Hoạt Nhảy Cao", -- Tên nút 1
        Script = function()
            game.Players.LocalPlayer.Character.Humanoid.JumpPower = 150
            print("Đã bật Nhảy Cao!")
        end
    },
    {
        Name = "Kích Hoạt Chạy Nhanh", -- Tên nút 2
        Script = function()
            game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = 100
            print("Đã bật Chạy Nhanh!")
        end
    },
    {
        Name = "Nhận Bản Đồ / Tool", -- Tên nút 3
        Script = function()
            -- Cho phép chạy script dạng loadstring từ bên ngoài
            -- loadstring(game:HttpGet("https://raw.githubusercontent.com/..."))()
            print("Đã chạy script bên thứ ba thành công!")
        end
    },
    {
        Name = "Nút Poine Băng",
        Script = function()
             -- ==========================================
-- TELEPORT CYCLE SYSTEM
-- CLICK -> TELEPORT -> NEXT POINT
-- ==========================================

local PATH_POSITIONS = {

    Vector3.new(-55.39, 202.12, -48.71), -- Point 1
    Vector3.new(86.31, 202.12, -35.57), -- Point 2
    Vector3.new(-35.48, 202.12, -10.70), -- Point 3
    Vector3.new(67.23, 202.18, -10.35), -- Point 4
    Vector3.new(18.54, 202.12, -50.85), -- Point 5
    Vector3.new(13.40, 202.12, -44.00), -- Point 6
    Vector3.new(17.40, 202.12, -44.00), -- Point 7
    Vector3.new(22.40, 202.12, -44.00), -- Point 8
    Vector3.new(17.40, 202.12, -40.00), -- Point 9
    Vector3.new(85.89, 202.12, -60.25), -- Point 10
    Vector3.new(65.89, 202.12, -60.25), -- Point 11
    Vector3.new(65.01, 202.12, -38.78), -- Point 12
}

-- Góc nhìn chung cho mọi điểm
local CAMERA_LOOK = Vector3.new(0.00, -0.98, -0.17)

-- ==========================================
-- GUI SETTINGS
-- ==========================================

local BUTTON_COLOR = Color3.fromRGB(255, 85, 85)

local BUTTON_SIZE = UDim2.new(0, 105, 0, 40)

local BUTTON_POSITION = UDim2.new(0.05, 0, 0.4, 0)

-- ==========================================
-- SERVICES
-- ==========================================

local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- ==========================================
-- REMOVE OLD GUI
-- ==========================================

pcall(function()
    if PlayerGui:FindFirstChild("TeleportCycleGui") then
        PlayerGui.TeleportCycleGui:Destroy()
    end
end)

-- ==========================================
-- CREATE GUI
-- ==========================================

local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "TeleportCycleGui"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = PlayerGui

local TeleportButton = Instance.new("TextButton")
TeleportButton.Name = "CycleButton"

TeleportButton.Size = BUTTON_SIZE
TeleportButton.Position = BUTTON_POSITION

TeleportButton.BackgroundColor3 = BUTTON_COLOR
TeleportButton.BorderSizePixel = 0

TeleportButton.TextColor3 = Color3.fromRGB(255,255,255)

TeleportButton.Font = Enum.Font.SourceSansBold
TeleportButton.TextSize = 16

TeleportButton.Active = true
TeleportButton.Draggable = true
TeleportButton.AutoButtonColor = true

TeleportButton.Parent = ScreenGui

-- Góc bo tròn
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = TeleportButton

-- Viền
local UIStroke = Instance.new("UIStroke")
UIStroke.Thickness = 1.5
UIStroke.Color = Color3.fromRGB(255,255,255)
UIStroke.Parent = TeleportButton

-- ==========================================
-- SYSTEM
-- ==========================================

local currentPoint = 1
local isTeleporting = false

-- Update text nút
local function updateButtonText()
    TeleportButton.Text = "Point " .. tostring(currentPoint)
end

updateButtonText()

-- ==========================================
-- TELEPORT FUNCTION
-- ==========================================

local function teleportToNextPoint()

    if isTeleporting then
        return
    end

    isTeleporting = true

    local character = LocalPlayer.Character

    if not character then
        isTeleporting = false
        return
    end

    local humanoidRootPart = character:FindFirstChild("HumanoidRootPart")

    local camera = Workspace.CurrentCamera

    if humanoidRootPart and camera then

        -- Lấy vị trí hiện tại
        local targetPosition = PATH_POSITIONS[currentPoint]

        -- Dịch chuyển
        humanoidRootPart.CFrame = CFrame.new(targetPosition)

        -- Chờ ổn định
        task.wait(0.05)

        -- Khóa góc nhìn first person
        LocalPlayer.CameraMode = Enum.CameraMode.LockFirstPerson

        -- Ép góc nhìn camera
        local camPos = camera.CFrame.Position

        camera.CFrame = CFrame.lookAt(
            camPos,
            camPos + CAMERA_LOOK
        )

        -- Chuyển sang điểm tiếp theo
        currentPoint += 1

        -- Quay vòng lại point 1
        if currentPoint > #PATH_POSITIONS then
            currentPoint = 1
        end

        -- Update text
        updateButtonText()
    end

    isTeleporting = false
end

-- ==========================================
-- BUTTON CLICK
-- ==========================================

TeleportButton.MouseButton1Click:Connect(function()

    teleportToNextPoint()

end)

-- ==========================================
-- CHARACTER RESPAWN SUPPORT
-- ==========================================

LocalPlayer.CharacterAdded:Connect(function()

    task.wait(1)

    LocalPlayer.CameraMode = Enum.CameraMode.Classic

end)
            print("Script 4 hoạt động!")
        end
    },
    {
        Name = "Nút Update 12",
        Script = function()
             -- Khai báo hệ thống
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")

local localPlayer = Players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")
local isAutoUpgrading = false

----------------------------------------------------------------
-- 1. TẠO GIAO DIỆN
----------------------------------------------------------------

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AbsoluteUpgradeGui"

pcall(function()
    screenGui.Parent = CoreGui
end)

if not screenGui.Parent then
    screenGui.Parent = playerGui
end

local mainButton = Instance.new("TextButton")
mainButton.Size = UDim2.new(0, 140, 0, 45)
mainButton.Position = UDim2.new(0.1, 0, 0.4, 0)
mainButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
mainButton.Text = "Auto 1-12: OFF" -- Đã sửa thành 1-12
mainButton.TextColor3 = Color3.fromRGB(255,255,255)
mainButton.Font = Enum.Font.GothamBold
mainButton.TextSize = 13
mainButton.Active = true
mainButton.Parent = screenGui

----------------------------------------------------------------
-- FIX DRAG CHUẨN
----------------------------------------------------------------

local dragging = false
local dragInput = nil
local dragStart = nil
local startPos = nil

local function updateDrag(input)
    local delta = input.Position - dragStart

    mainButton.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

mainButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then

        dragging = true
        dragInput = input
        dragStart = input.Position
        startPos = mainButton.Position

        -- Theo dõi khi thả tay
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                dragInput = nil
            end
        end)
    end
end)

mainButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input == dragInput then
        updateDrag(input)
    end
end)

----------------------------------------------------------------
-- 2. LOGIC NÂNG CẤP (1-12)
----------------------------------------------------------------

local function forceClick(button)
    if button then
        pcall(function()
            for _, connection in pairs(getconnections(button.MouseButton1Click)) do
                connection:Fire()
            end

            for _, connection in pairs(getconnections(button.MouseButton1Down)) do
                connection:Fire()
            end

            for _, connection in pairs(getconnections(button.Activated)) do
                connection:Fire()
            end
        end)
    end
end

local function getUpgradeButton(towerIndex)
    local targetPath = {
        "MainHud",
        "Menus",
        "TeamManager",
        "Menu",
        "Towers",
        "Listings",
        tostring(towerIndex),
        "UpgradeButtons",
        "Upgrade"
    }

    local current = playerGui

    for _, name in ipairs(targetPath) do
        if current then
            current = current:FindFirstChild(name)
        else
            return nil
        end
    end

    return current
end

task.spawn(function()
    while true do
        if isAutoUpgrading then
            for i = 1, 12 do -- Đã sửa vòng lặp lên 12
                if isAutoUpgrading then
                    local upgradeBtn = getUpgradeButton(i)

                    if upgradeBtn then
                        forceClick(upgradeBtn)
                    end

                    task.wait(0.3)
                end
            end
        else
            task.wait(0.5)
        end
    end
end)

----------------------------------------------------------------
-- 3. BẬT / TẮT
----------------------------------------------------------------

mainButton.MouseButton1Click:Connect(function()
    if dragging then
        return
    end

    isAutoUpgrading = not isAutoUpgrading

    if isAutoUpgrading then
        mainButton.BackgroundColor3 = Color3.fromRGB(46,204,113)
        mainButton.Text = "Auto 1-12: ON" -- Đã sửa thành 1-12
    else
        mainButton.BackgroundColor3 = Color3.fromRGB(231,76,60)
        mainButton.Text = "Auto 1-12: OFF" -- Đã sửa thành 1-12
    end
end)

print("Fixed drag bug hoàn toàn và đã nâng cấp từ 1-12!")

            print("Script 5 hoạt động!")
        end
    },
    {
        Name = "Nút Update 6",
        Script = function()
             -- Khai báo hệ thống
local Players = game:GetService("Players")
local TweenService = game:GetService("TweenService")
local CoreGui = game:GetService("CoreGui")
local UserInputService = game:GetService("UserInputService")

local localPlayer = Players.LocalPlayer
local playerGui = localPlayer:WaitForChild("PlayerGui")
local isAutoUpgrading = false

----------------------------------------------------------------
-- 1. TẠO GIAO DIỆN
----------------------------------------------------------------

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "AbsoluteUpgradeGui"

pcall(function()
    screenGui.Parent = CoreGui
end)

if not screenGui.Parent then
    screenGui.Parent = playerGui
end

local mainButton = Instance.new("TextButton")
mainButton.Size = UDim2.new(0, 140, 0, 45)
mainButton.Position = UDim2.new(0.1, 0, 0.4, 0)
mainButton.BackgroundColor3 = Color3.fromRGB(231, 76, 60)
mainButton.Text = "Auto 1-6: OFF"
mainButton.TextColor3 = Color3.fromRGB(255,255,255)
mainButton.Font = Enum.Font.GothamBold
mainButton.TextSize = 13
mainButton.Active = true
mainButton.Parent = screenGui

----------------------------------------------------------------
-- FIX DRAG CHUẨN
----------------------------------------------------------------

local dragging = false
local dragInput = nil
local dragStart = nil
local startPos = nil

local function updateDrag(input)
    local delta = input.Position - dragStart

    mainButton.Position = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
end

mainButton.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1
    or input.UserInputType == Enum.UserInputType.Touch then

        dragging = true
        dragInput = input
        dragStart = input.Position
        startPos = mainButton.Position

        -- Theo dõi khi thả tay
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                dragInput = nil
            end
        end)
    end
end)

mainButton.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement
    or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and input == dragInput then
        updateDrag(input)
    end
end)

----------------------------------------------------------------
-- 2. LOGIC NÂNG CẤP (1-6)
----------------------------------------------------------------

local function forceClick(button)
    if button then
        pcall(function()
            for _, connection in pairs(getconnections(button.MouseButton1Click)) do
                connection:Fire()
            end

            for _, connection in pairs(getconnections(button.MouseButton1Down)) do
                connection:Fire()
            end

            for _, connection in pairs(getconnections(button.Activated)) do
                connection:Fire()
            end
        end)
    end
end

local function getUpgradeButton(towerIndex)
    local targetPath = {
        "MainHud",
        "Menus",
        "TeamManager",
        "Menu",
        "Towers",
        "Listings",
        tostring(towerIndex),
        "UpgradeButtons",
        "Upgrade"
    }

    local current = playerGui

    for _, name in ipairs(targetPath) do
        if current then
            current = current:FindFirstChild(name)
        else
            return nil
        end
    end

    return current
end

task.spawn(function()
    while true do
        if isAutoUpgrading then
            for i = 1,6 do
                if isAutoUpgrading then
                    local upgradeBtn = getUpgradeButton(i)

                    if upgradeBtn then
                        forceClick(upgradeBtn)
                    end

                    task.wait(0.3)
                end
            end
        else
            task.wait(0.5)
        end
    end
end)

----------------------------------------------------------------
-- 3. BẬT / TẮT
----------------------------------------------------------------

mainButton.MouseButton1Click:Connect(function()
    if dragging then
        return
    end

    isAutoUpgrading = not isAutoUpgrading

    if isAutoUpgrading then
        mainButton.BackgroundColor3 = Color3.fromRGB(46,204,113)
        mainButton.Text = "Auto 1-6: ON"
    else
        mainButton.BackgroundColor3 = Color3.fromRGB(231,76,60)
        mainButton.Text = "Auto 1-6: OFF"
    end
end)

print("Fixed drag bug hoàn toàn!")
            print("Script 6 hoạt động!")
        end
    },
    {
        Name = "Nút 4 Tổ Hợp",
        Script = function()
             loadstring(game:HttpGet("https://raw.githubusercontent.com/tueluanm-stack/luaroblox/refs/heads/main/thehouse4"))()
            print("Script 4 hoạt động!")
        end
    },
    {
        Name = "Nút AFK",
        Script = function()
             loadstring(game:HttpGet("https://raw.githubusercontent.com/tueluanm-stack/luaroblox/refs/heads/main/Players.LocalPlayer.AntiAfk"))()
            print("Script pp5 hoạt động!")
        end
    },
    {
        Name = "Nút Thử Nghiệm 9",
        Script = function()
            print("Script 6 hoạt động!")
        end
    },
    -- Bạn có thể Copy thêm các hàng tương tự như trên xuống dưới này để tạo thêm nút, danh sách sẽ tự cuộn dài ra!
}

-- =============================================================================
-- [ HỆ THỐNG TẠO GIAO DIỆN (GUI ENGINE) - ĐÃ SỬA LỖI ]
-- =============================================================================

local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Players = game:GetService("Players")

local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

-- Xóa menu cũ nếu có
pcall(function()
    local oldGui = PlayerGui:FindFirstChild("Gemini_CustomMenu")
    if oldGui then
        oldGui:Destroy()
    end
end)

-- Tạo ScreenGui
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "Gemini_CustomMenu"
ScreenGui.ResetOnSpawn = false
ScreenGui.IgnoreGuiInset = true
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui

--------------------------------------------------
-- MAIN FRAME
--------------------------------------------------

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 320, 0, 380)
MainFrame.Position = UDim2.new(0.5, -160, 0.5, -190)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local MainCorner = Instance.new("UICorner")
MainCorner.CornerRadius = UDim.new(0, 10)
MainCorner.Parent = MainFrame

local MainStroke = Instance.new("UIStroke")
MainStroke.Thickness = 1
MainStroke.Color = Color3.fromRGB(55, 55, 55)
MainStroke.Parent = MainFrame

--------------------------------------------------
-- TITLE BAR (CHỈ KÉO Ở ĐÂY)
--------------------------------------------------

local TitleBar = Instance.new("Frame")
TitleBar.Name = "TitleBar"
TitleBar.Size = UDim2.new(1, 0, 0, 45)
TitleBar.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleBar.BorderSizePixel = 0
TitleBar.Active = true
TitleBar.Parent = MainFrame

local TitleBarCorner = Instance.new("UICorner")
TitleBarCorner.CornerRadius = UDim.new(0, 10)
TitleBarCorner.Parent = TitleBar

local TitleCover = Instance.new("Frame")
TitleCover.Name = "TitleCover"
TitleCover.Size = UDim2.new(1, 0, 0, 10)
TitleCover.Position = UDim2.new(0, 0, 1, -10)
TitleCover.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
TitleCover.BorderSizePixel = 0
TitleCover.Parent = TitleBar

local TitleText = Instance.new("TextLabel")
TitleText.Name = "TitleText"
TitleText.Size = UDim2.new(1, -50, 1, 0)
TitleText.Position = UDim2.new(0, 12, 0, 0)
TitleText.BackgroundTransparency = 1
TitleText.Text = MenuTitle
TitleText.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleText.TextSize = 16
TitleText.Font = Enum.Font.SourceSansBold
TitleText.TextXAlignment = Enum.TextXAlignment.Left
TitleText.Parent = TitleBar

--------------------------------------------------
-- CLOSE BUTTON
--------------------------------------------------

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 30, 0, 30)
CloseButton.Position = UDim2.new(1, -40, 0, 7)
CloseButton.BackgroundColor3 = Color3.fromRGB(220, 60, 60)
CloseButton.Text = "×"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 22
CloseButton.Font = Enum.Font.SourceSansBold
CloseButton.BorderSizePixel = 0
CloseButton.Parent = TitleBar

local CloseCorner = Instance.new("UICorner")
CloseCorner.CornerRadius = UDim.new(0, 6)
CloseCorner.Parent = CloseButton

CloseButton.MouseButton1Click:Connect(function()
    if ScreenGui then
        ScreenGui:Destroy()
    end
end)

--------------------------------------------------
-- SCROLL FRAME
--------------------------------------------------

local ScrollFrame = Instance.new("ScrollingFrame")
ScrollFrame.Name = "ScrollFrame"
ScrollFrame.Size = UDim2.new(1, -20, 1, -65)
ScrollFrame.Position = UDim2.new(0, 10, 0, 50)
ScrollFrame.BackgroundTransparency = 1
ScrollFrame.BorderSizePixel = 0
ScrollFrame.ScrollBarThickness = 6
ScrollFrame.ScrollBarImageColor3 = Color3.fromRGB(100, 100, 100)
ScrollFrame.ScrollingDirection = Enum.ScrollingDirection.Y
ScrollFrame.Active = true
ScrollFrame.Selectable = false
ScrollFrame.ClipsDescendants = true
ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, 0)
ScrollFrame.Parent = MainFrame

local Padding = Instance.new("UIPadding")
Padding.PaddingTop = UDim.new(0, 2)
Padding.PaddingBottom = UDim.new(0, 8)
Padding.PaddingLeft = UDim.new(0, 2)
Padding.PaddingRight = UDim.new(0, 2)
Padding.Parent = ScrollFrame

local UIListLayout = Instance.new("UIListLayout")
UIListLayout.SortOrder = Enum.SortOrder.LayoutOrder
UIListLayout.Padding = UDim.new(0, 8)
UIListLayout.Parent = ScrollFrame

local function refreshCanvas()
    ScrollFrame.CanvasSize = UDim2.new(0, 0, 0, UIListLayout.AbsoluteContentSize.Y + 10)
end

UIListLayout:GetPropertyChangedSignal("AbsoluteContentSize"):Connect(refreshCanvas)
task.defer(refreshCanvas)

--------------------------------------------------
-- DEBUG COUNT
--------------------------------------------------

print("So nut =", #MenuConfig)

--------------------------------------------------
-- BUTTONS
--------------------------------------------------

for index, config in ipairs(MenuConfig) do
    local Button = Instance.new("TextButton")
    Button.Name = "ScriptButton_" .. index
    Button.LayoutOrder = index
    Button.Size = UDim2.new(1, -10, 0, 42)
    Button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    Button.BorderSizePixel = 0
    Button.Text = tostring(config.Name or ("Nút " .. index))
    Button.TextColor3 = Color3.fromRGB(230, 230, 230)
    Button.TextSize = 14
    Button.Font = Enum.Font.SourceSansSemibold
    Button.AutoButtonColor = true
    Button.Parent = ScrollFrame

    local ButtonCorner = Instance.new("UICorner")
    ButtonCorner.CornerRadius = UDim.new(0, 6)
    ButtonCorner.Parent = Button

    local ButtonStroke = Instance.new("UIStroke")
    ButtonStroke.Thickness = 1
    ButtonStroke.Color = Color3.fromRGB(55, 55, 55)
    ButtonStroke.Parent = Button

    local isClicked = false

    Button.MouseButton1Click:Connect(function()
        if isClicked then
            return
        end

        isClicked = true

        Button.BackgroundColor3 = Color3.fromRGB(20, 50, 30)
        Button.Text = "[Đã Bật] " .. tostring(config.Name or ("Nút " .. index))
        Button.TextColor3 = Color3.fromRGB(140, 140, 140)

        task.spawn(function()
            local success, err = pcall(function()
                if type(config.Script) == "function" then
                    config.Script()
                else
                    error("Script không hợp lệ")
                end
            end)

            if not success then
                warn("Lỗi khi chạy nút " .. tostring(config.Name or index) .. ": " .. tostring(err))
                Button.BackgroundColor3 = Color3.fromRGB(120, 30, 30)
                Button.Text = "[LỖI] " .. tostring(config.Name or ("Nút " .. index))
                Button.TextColor3 = Color3.fromRGB(255, 220, 220)
                isClicked = false
            end
        end)
    end)
end

task.defer(refreshCanvas)

--------------------------------------------------
-- DRAG SYSTEM: CHỈ KÉO Ở TITLE BAR
--------------------------------------------------

local dragging = false
local dragStart
local startPos
local dragInput

local function updateDrag(input)
    local delta = input.Position - dragStart
    local newPos = UDim2.new(
        startPos.X.Scale,
        startPos.X.Offset + delta.X,
        startPos.Y.Scale,
        startPos.Y.Offset + delta.Y
    )
    MainFrame.Position = newPos
end

TitleBar.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        dragInput = input

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                dragInput = nil
            end
        end)
    end
end)

TitleBar.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if dragging and input == dragInput then
        updateDrag(input)
    end
end)
